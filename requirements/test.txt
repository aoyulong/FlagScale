# Testing dependencies
# Includes pytest framework, coverage tools, and testing utilities
# Versions are defined in versions.json (dev section, pip: true)

coverage==7.6.10
pytest==8.3.5
pytest-asyncio==0.25.3
pytest-cov==6.0.0
pytest-mock==3.14.0
pytest-random-order==1.1.1
